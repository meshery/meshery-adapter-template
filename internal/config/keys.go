package config

const (

	// Operation keys
	installMesh   = "installMesh"   // installMesh is the key to install <adaptor-name>
	installSample = "installSample" // installSample is the key to install sample application
)
